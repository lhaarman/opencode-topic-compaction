# Dependencies, build artifacts and local state
node_modules
package-lock.json
*.tsbuildinfo

# opencode we can add manually if we desire so
.opencode/

# OS/editor noise
.DS_Store
Thumbs.db

# Local review notes and feedback drafts
agent-feedback.txt
feedback_chatbot.md

# Stale rendering output (feature removed)
graph-poc/
